# Phi 家族在 AITK

[AI Toolkit for VS Code](https://marketplace.visualstudio.com/items?itemName=ms-windows-ai-studio.windows-ai-studio) 透過集合 Microsoft Foundry Catalog 及其他目錄如 Hugging Face 的尖端 AI 開發工具及模型，簡化了生成式 AI 應用程式開發。您可以瀏覽由 GitHub Models 和 Microsoft Foundry Model Catalogs 支援的 AI 模型目錄，下載至本地或遠端，進行微調、測試並在您的應用程式中使用它們。

AI Toolkit 預覽版將於本地運行。根據您選擇的模型，是否進行本地推理或微調，您可能需要配備如 NVIDIA CUDA GPU 的 GPU。您也可直接用 AITK 運行 GitHub Models。

## 入門

[了解如何安裝 Windows 子系統 for Linux](https://learn.microsoft.com/windows/wsl/install?WT.mc_id=aiml-137032-kinfeylo)

及 [更改預設發行版](https://learn.microsoft.com/windows/wsl/install#change-the-default-linux-distribution-installed)。

[AI Toolkit GitHub 倉庫](https://github.com/microsoft/vscode-ai-toolkit/)

- Windows、Linux、macOS
  
- 在 Windows 和 Linux 進行微調時，您需要 Nvidia GPU。此外，**Windows** 需要安裝 Ubuntu 18.4 或以上版本的 Linux 子系統。 [了解如何安裝 Windows 子系統 for Linux](https://learn.microsoft.com/windows/wsl/install) 及 [更改預設發行版](https://learn.microsoft.com/windows/wsl/install#change-the-default-linux-distribution-installed)。

### 安裝 AI Toolkit

AI Toolkit 以 [Visual Studio Code 擴展](https://code.visualstudio.com/docs/setup/additional-components#_vs-code-extensions) 的形式發佈，所以您需要先安裝 [VS Code](https://code.visualstudio.com/docs/setup/windows?WT.mc_id=aiml-137032-kinfeylo)，然後從 [VS Marketplace](https://marketplace.visualstudio.com/items?itemName=ms-windows-ai-studio.windows-ai-studio) 下載 AI Toolkit。
[AI Toolkit 已在 Visual Studio Marketplace 中提供](https://marketplace.visualstudio.com/items?itemName=ms-windows-ai-studio.windows-ai-studio)，可以像安裝其他 VS Code 擴展一樣安裝。

如果您對安裝 VS Code 擴展不熟悉，請遵循下列步驟：

### 登入

1. 在 VS Code 的活動欄中選擇 <strong>擴展</strong>  
1. 在擴展搜尋欄輸入 “AI Toolkit”  
1. 選擇「AI Toolkit for Visual Studio code」  
1. 點擊 <strong>安裝</strong>

現在，您已準備好使用該擴展！

系統將提示您登入 GitHub，請點擊「允許」繼續。您將被導向 GitHub 登入頁面。

請登入並依流程操作。成功完成後，您將被重導回 VS Code。

擴展安裝完成後，您會在活動欄看到 AI Toolkit 的圖示。

讓我們探索可用的操作！

### 可用操作

AI Toolkit 的主要側邊欄組成為：

- **Models**
- **Resources**
- **Playground**  
- **Fine-tuning**
- **Evaluation**

以上在 Resources 區段都有提供。要開始使用，請選擇 **Model Catalog**。

### 從目錄下載模型

從 VS Code 側邊欄啟動 AI Toolkit 後，您可以選擇以下選項：

![AI toolkit model catalog](../../../../../translated_images/zh-HK/AItoolkitmodel_catalog.7a7be6a7d8468d31.webp)

- 從 **Model Catalog** 找到支援的模型並下載至本地  
- 在 **Model Playground** 測試模型推理  
- 在 **Model Fine-tuning** 本地或遠端微調模型  
- 透過 AI Toolkit 命令面板將微調後模型部署到雲端  
- 評估模型

> [!NOTE]
>
> **GPU Vs CPU**
>
> 你會發現模型卡上會顯示模型大小、平台和加速類型（CPU，GPU）。若在 **Windows 裝置且至少有一個 GPU**，建議選擇僅針對 Windows 優化的模型版本以獲得最佳效能。
>
> 這可確保您使用的是為 DirectML 加速器優化的模型。
>
> 模型名稱格式為
>
> - `{model_name}-{accelerator}-{quantization}-{format}`。
>
>要檢查您的 Windows 裝置是否有 GPU，請開啟 <strong>工作管理員</strong>，然後選擇 <strong>效能</strong> 分頁。若有 GPU，將會以「GPU 0」或「GPU 1」等名稱列出。

### 在遊樂場執行模型

設置完所有參數後，點擊 **Generate Project**。

模型下載完成後，在目錄中的模型卡選擇 **Load in Playground**：

- 啟動模型下載  
- 安裝所有先決條件和依賴項  
- 建立 VS Code 工作區

![Load model in playground](../../../../../translated_images/zh-HK/AItoolkitload_model_into_playground.dcef5355b1653b52.webp)

### 在您的應用程式中使用 REST API

AI Toolkit 搭載了本地 REST API 網頁伺服器，**監聽端口 5272**，使用的是 [OpenAI chat completions 格式](https://platform.openai.com/docs/api-reference/chat/create)。

這讓您能夠在本地測試應用程式，而無需依賴雲端 AI 模型服務。例如，以下 JSON 文件展示了如何配置請求的主體：

```json
{
    "model": "Phi-4",
    "messages": [
        {
            "role": "user",
            "content": "what is the golden ratio?"
        }
    ],
    "temperature": 0.7,
    "top_p": 1,
    "top_k": 10,
    "max_tokens": 100,
    "stream": true
}
```
  
您可以使用（例如）[Postman](https://www.postman.com/) 或 CURL（Client URL）工具測試 REST API：

```bash
curl -vX POST http://127.0.0.1:5272/v1/chat/completions -H 'Content-Type: application/json' -d @body.json
```
  
### 使用 Python 的 OpenAI 客戶端庫

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://127.0.0.1:5272/v1/", 
    api_key="x" # API 所需但未使用
)

chat_completion = client.chat.completions.create(
    messages=[
        {
            "role": "user",
            "content": "what is the golden ratio?",
        }
    ],
    model="Phi-4",
)

print(chat_completion.choices[0].message.content)
```
  
### 使用 .NET 的 Azure OpenAI 客戶端庫

使用 NuGet 向您的專案添加 [Azure OpenAI 客戶端庫 for .NET](https://www.nuget.org/packages/Azure.AI.OpenAI/)：

```bash
dotnet add {project_name} package Azure.AI.OpenAI --version 1.0.0-beta.17
```
  
新增名稱為 **OverridePolicy.cs** 的 C# 檔案至專案，並貼上以下代碼：

```csharp
// OverridePolicy.cs
using Azure.Core.Pipeline;
using Azure.Core;

internal partial class OverrideRequestUriPolicy(Uri overrideUri)
    : HttpPipelineSynchronousPolicy
{
    private readonly Uri _overrideUri = overrideUri;

    public override void OnSendingRequest(HttpMessage message)
    {
        message.Request.Uri.Reset(_overrideUri);
    }
}
```
  
接著，將以下代碼貼至您的 **Program.cs** 檔案：

```csharp
// Program.cs
using Azure.AI.OpenAI;

Uri localhostUri = new("http://localhost:5272/v1/chat/completions");

OpenAIClientOptions clientOptions = new();
clientOptions.AddPolicy(
    new OverrideRequestUriPolicy(localhostUri),
    Azure.Core.HttpPipelinePosition.BeforeTransport);
OpenAIClient client = new(openAIApiKey: "unused", clientOptions);

ChatCompletionsOptions options = new()
{
    DeploymentName = "Phi-4",
    Messages =
    {
        new ChatRequestSystemMessage("You are a helpful assistant. Be brief and succinct."),
        new ChatRequestUserMessage("What is the golden ratio?"),
    }
};

StreamingResponse<StreamingChatCompletionsUpdate> streamingChatResponse
    = await client.GetChatCompletionsStreamingAsync(options);

await foreach (StreamingChatCompletionsUpdate chatChunk in streamingChatResponse)
{
    Console.Write(chatChunk.ContentUpdate);
}
```


## 使用 AI Toolkit 進行微調

- 從模型探索和遊樂場開始  
- 使用本地計算資源進行模型微調和推理  
- 使用 Azure 資源進行遠端微調和推理

[使用 AI Toolkit 進行微調](../../03.FineTuning/Finetuning_VSCodeaitoolkit.md)

## AI Toolkit 問答資源

有關最常見問題及解決方案，請參閱我們的 [問答頁面](https://github.com/microsoft/vscode-ai-toolkit/blob/main/archive/QA.md)

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**免責聲明**：  
本文件由 AI 翻譯服務 [Co-op Translator](https://github.com/Azure/co-op-translator) 進行翻譯。雖然我們致力於保持準確性，但請注意自動翻譯可能包含錯誤或不準確之處。原文文件的母語版本應被視為權威來源。對於重要資訊，建議採用專業人工翻譯。我們不對因使用本翻譯而產生的任何誤解或誤釋負責。
<!-- CO-OP TRANSLATOR DISCLAIMER END -->