# Phi Family for AITK

[AI Toolkit for VS Code](https://marketplace.visualstudio.com/items?itemName=ms-windows-ai-studio.windows-ai-studio) dey simplify generative AI app development by bring together cutting-edge AI development tools and models from Microsoft Foundry Catalog and oda catalogs like Hugging Face. You go fit browse di AI models catalog wey powered by GitHub Models and Microsoft Foundry Model Catalogs, download dem locally or remote, fine-tune dem, test and use dem for your app.

AI Toolkit Preview go run for your machine. Whether na local inference or fine-tune, e depend on di model wey you choose, you fit need GPU like NVIDIA CUDA GPU. You fit run GitHub Models directly with AITK too.

## How to Start

[Learn more how to install Windows subsystem for Linux](https://learn.microsoft.com/windows/wsl/install?WT.mc_id=aiml-137032-kinfeylo)

and [changing default distribution](https://learn.microsoft.com/windows/wsl/install#change-the-default-linux-distribution-installed).

[AI Toolkit GitHub Repo](https://github.com/microsoft/vscode-ai-toolkit/)

- Windows, Linux, macOS
  
- For finetuning for both Windows and Linux, you go need Nvidia GPU. Also, **Windows** need subsystem for Linux with Ubuntu distro 18.4 or newer. [Learn more how to install Windows subsystem for Linux](https://learn.microsoft.com/windows/wsl/install) and [changing default distribution](https://learn.microsoft.com/windows/wsl/install#change-the-default-linux-distribution-installed).

### How to Install AI Toolkit

AI Toolkit dey delivered as a [Visual Studio Code Extension](https://code.visualstudio.com/docs/setup/additional-components#_vs-code-extensions), so you go need install [VS Code](https://code.visualstudio.com/docs/setup/windows?WT.mc_id=aiml-137032-kinfeylo) first, then download AI Toolkit from di [VS Marketplace](https://marketplace.visualstudio.com/items?itemName=ms-windows-ai-studio.windows-ai-studio).
The [AI Toolkit dey available for the Visual Studio Marketplace](https://marketplace.visualstudio.com/items?itemName=ms-windows-ai-studio.windows-ai-studio) and you fit install am like any oda VS Code extension. 

If you no sabi how to install VS Code extensions, follow dis step:

### Sign In

1. For Activity Bar for VS Code, select **Extensions**
1. For Extensions Search bar, type "AI Toolkit"
1. Select di "AI Toolkit for Visual Studio code"
1. Select **Install**

Now you ready to use di extension!

You go get prompt to sign in to GitHub, so abeg click "Allow" to continue. You go redirect go GitHub signin page.

Abeg sign in and follow di steps wey dem give. After you gree finish, you go redirect back to VS Code.

Once di extension don install, you go see AI Toolkit icon show for your Activity Bar.

Make we explore di actions wey dey available!

### Di Actions Wey Dey Available

Di main sidebar wey AI Toolkit get dey organized into  

- **Models**
- **Resources**
- **Playground**  
- **Fine-tuning**
- **Evaluation**

Dem dey available for Resources section. To start, select **Model Catalog**.

### How to Download Model from di Catalog

When you don launch AI Toolkit from VS Code side bar, you fit select from these options:

![AI toolkit model catalog](../../../../../translated_images/pcm/AItoolkitmodel_catalog.7a7be6a7d8468d31.webp)

- Find model wey e dey support for **Model Catalog** and download am locally
- Test model inference for **Model Playground**
- Fine-tune di model locally or remotely for **Model Fine-tuning**
- Deploy fine-tuned models to cloud using command palette for AI Toolkit
- Evaluation models

> [!NOTE]
>
> **GPU vs CPU**
>
> You go notice say di model cards dey show model size, platform, and accelerator type (CPU, GPU). For better performance on **Windows devices wey get at least one GPU**, select model versions wey only target Windows.
>
> Dis one go make sure say you get model wey optimize for DirectML accelerator.
>
> Di model names go follow dis format:
>
> - `{model_name}-{accelerator}-{quantization}-{format}`.
>
>To check if your Windows device get GPU, open **Task Manager** and then select di **Performance** tab. If you get GPU(s), dem go show under names like "GPU 0" or "GPU 1".

### How to Run Model for di Playground

After you don set all di parameters, click **Generate Project**.

After your model don download, select **Load in Playground** for di model card wey dey the catalog:

- Start model download
- Install all prerequisites and dependencies
- Create VS Code workspace

![Load model in playground](../../../../../translated_images/pcm/AItoolkitload_model_into_playground.dcef5355b1653b52.webp)

### Use di REST API for your App

Di AI Toolkit come with local REST API web server **on port 5272** wey dey use di [OpenAI chat completions format](https://platform.openai.com/docs/api-reference/chat/create). 

Dis one go make you fit test your app locally without need use cloud AI model service. Example, dis JSON file show how to configure di body of di request:

```json
{
    "model": "Phi-4",
    "messages": [
        {
            "role": "user",
            "content": "what is the golden ratio?"
        }
    ],
    "temperature": 0.7,
    "top_p": 1,
    "top_k": 10,
    "max_tokens": 100,
    "stream": true
}
```

You fit test di REST API using (like) [Postman](https://www.postman.com/) or di CURL (Client URL) utility:

```bash
curl -vX POST http://127.0.0.1:5272/v1/chat/completions -H 'Content-Type: application/json' -d @body.json
```

### Using di OpenAI client library for Python

```python
from openai import OpenAI

client = OpenAI(
    base_url="http://127.0.0.1:5272/v1/", 
    api_key="x" # e need for di API but e no dey used
)

chat_completion = client.chat.completions.create(
    messages=[
        {
            "role": "user",
            "content": "what is the golden ratio?",
        }
    ],
    model="Phi-4",
)

print(chat_completion.choices[0].message.content)
```

### Using Azure OpenAI client library for .NET

Add di [Azure OpenAI client library for .NET](https://www.nuget.org/packages/Azure.AI.OpenAI/) to your project using NuGet:

```bash
dotnet add {project_name} package Azure.AI.OpenAI --version 1.0.0-beta.17
```

Add one C# file wey you go call **OverridePolicy.cs** to your project and paste dis code inside:

```csharp
// OverridePolicy.cs
using Azure.Core.Pipeline;
using Azure.Core;

internal partial class OverrideRequestUriPolicy(Uri overrideUri)
    : HttpPipelineSynchronousPolicy
{
    private readonly Uri _overrideUri = overrideUri;

    public override void OnSendingRequest(HttpMessage message)
    {
        message.Request.Uri.Reset(_overrideUri);
    }
}
```

Next, paste dis code inside your **Program.cs** file:

```csharp
// Program.cs
using Azure.AI.OpenAI;

Uri localhostUri = new("http://localhost:5272/v1/chat/completions");

OpenAIClientOptions clientOptions = new();
clientOptions.AddPolicy(
    new OverrideRequestUriPolicy(localhostUri),
    Azure.Core.HttpPipelinePosition.BeforeTransport);
OpenAIClient client = new(openAIApiKey: "unused", clientOptions);

ChatCompletionsOptions options = new()
{
    DeploymentName = "Phi-4",
    Messages =
    {
        new ChatRequestSystemMessage("You are a helpful assistant. Be brief and succinct."),
        new ChatRequestUserMessage("What is the golden ratio?"),
    }
};

StreamingResponse<StreamingChatCompletionsUpdate> streamingChatResponse
    = await client.GetChatCompletionsStreamingAsync(options);

await foreach (StreamingChatCompletionsUpdate chatChunk in streamingChatResponse)
{
    Console.Write(chatChunk.ContentUpdate);
}
```


## Fine Tune Wit AI Toolkit

- Start with model discovery and playground.
- Model fine-tuning and inference using local computer resources.
- Remote fine-tuning and inference using Azure resources

[Fine Tuning with AI Toolkit](../../03.FineTuning/Finetuning_VSCodeaitoolkit.md)

## AI Toolkit Q&A Resources

Abeg check our [Q&A page](https://github.com/microsoft/vscode-ai-toolkit/blob/main/archive/QA.md) for most common issues and how to fix dem

---

<!-- CO-OP TRANSLATOR DISCLAIMER START -->
**Disclaimer**:  
Dis dokument don translate wit AI translation service [Co-op Translator](https://github.com/Azure/co-op-translator). Even though we dey try make am correct, abeg sabi say automated translate fit get errors or mistakes. Di original dokument for e native language na di correct source. For important mata, e better make person wey sabi translate am human do am. We no go responsible for any misunderstanding or misinterpretation wey fit happen because of dis translation.
<!-- CO-OP TRANSLATOR DISCLAIMER END -->